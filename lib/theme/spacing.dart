import 'package:flutter/material.dart';

const EdgeInsets screenPadding = EdgeInsets.symmetric(horizontal: 17);
const double slotGap = 7.0;
const BorderRadius cardRadius = BorderRadius.all(Radius.circular(18));
const BorderRadius slotRadius = BorderRadius.all(Radius.circular(12));
const BorderRadius btnRadius = BorderRadius.all(Radius.circular(18));
